core = "6.x"
api = "2"

; Rebuild with
;   1. cd example
;   2. ./rebuild.sh

; Includes ====================================================================

; Can be a relevant path.
;includes[] = "../../base.make"

includes[] = "https://github.com/makara/buildkit_plus_v6/raw/master/base.make"

; Modules =====================================================================

; Themes ======================================================================

; Libraries ===================================================================
